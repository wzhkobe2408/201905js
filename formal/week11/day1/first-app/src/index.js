// 使用 React
import React, { Component } from 'react'
import ReactDOM from 'react-dom'

// let h = <h1 id="xyz">HelloWorld111</h1> // jsx 语法
//
// ReactDOM.render(h, document.getElementById('root'), () => {
//   console.log('callback')
//   console.log(document.getElementById('xyz'))
// })
// ReactDOM.render (ele, container, callback)
// ele: 要渲染到页面的 jsx 元素 或者 组件
// container: 要把上面的 ele 渲染到哪个元素中，是一个 DOM 对象
// callback: 把 ele 渲染到页面后执行的回调函数，在这个回调函数中可以操作 DOM

// src/index.js 是程序的主入口
import './courses/6-createElement'
